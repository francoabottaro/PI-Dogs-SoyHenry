import { createSlice } from '@reduxjs/toolkit'

const temperamentsSlice = createSlice({
	name: 'temperaments',
	initialState: [],
	reducers: {}
})
export const {} = apiTemperamentsSlice.actions
export default temperamentsSlice.reducer
